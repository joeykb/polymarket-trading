apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tempedge-output-green
  namespace: tempedge
  labels:
    app: tempedge
    component: storage
    version: green
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: local-path
